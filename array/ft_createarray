#include "libft.h"

#define ARRAY_DEFAULT_ALLOC 1024 * sizeof(t_array)

t_mdata_array		ft_createarray(size_t nb_cases, size_t nb_alloc)
{
	t_mdata_array	md;

	md.start = ft_memalloc_fail(nb_alloc * ARRAY_DEFAULT_ALLOC);
	md.nb_alloc = nb_alloc;
	md.nb_cases = nb_cases;
	return (md);
}
